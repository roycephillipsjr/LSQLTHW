CREATE TABLE person (
  id INTEGER PRIMARY KEY,
  first_name TEXT,
  last_name TEXT,
  age INTEGER,
  location TEXT,
  job_title TEXT,
  income INTEGER
);

-- create table person (
--   id integer primary key,
--   first_name text,
--   last_name text,
--   age integer
-- );
